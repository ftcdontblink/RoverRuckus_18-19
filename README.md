# teamcode
The code was updated - this is the final code we are using


TODO:

MOUNTING ARM TODAY

TMSERVO CODE (REMOVED CURRENTLY):

   // when the Right Bumper on Gamepad 1 is pressed, bring the servo down
            if(gamepad1.right_bumper){
            	robot.tmServo.setPosition(0.75);

            	
            }
            // When the Left Bumper on Gamepad 1 is pressed, take the Servo back up
            if (gamepad1.left_bumper){
            	robot.tmServo.setPosition(0);

            	
            }


last update - 1/26/2019


